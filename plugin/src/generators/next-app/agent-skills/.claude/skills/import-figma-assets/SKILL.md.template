---
name: import-figma-assets
description: >
  Export the favicon and icon sprite from a Figma UI-kit page's "Web assets" block into this app's
  Next.js app and UI-kit library. Use whenever asked to "import figma assets/icons", "export the
  favicon from figma", "import the icon sprite", or similar.
---

# Import Figma assets

This is a pointer skill. The full instructions live at
`.agents/skills/nextjs/import-figma-assets/SKILL.md` (Agent Skills convention, shared across coding
agents) — read that file and follow it in full before doing any asset export work.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/nextjs/`, not in this one.
