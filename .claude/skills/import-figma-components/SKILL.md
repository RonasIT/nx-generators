---
name: import-figma-components
description: >
  Implement Button (`AppButton`), Input (`TextInput`), and Checkbox from a Figma UI-kit page's
  "Buttons" / "Inputs" / "Checkboxes and Radio Buttons" blocks into this app's UI-kit library,
  detecting the project's UI library first. Use whenever asked to "import figma components",
  "implement the Button/Input/Checkbox from Figma", "generate UI-kit components from Figma", or
  similar.
---

# Import Figma components

This is a pointer skill. The full instructions live at
`.agents/skills/nextjs/import-figma-components/SKILL.md` (Agent Skills convention, shared across
coding agents) — read that file and follow it in full before doing any Figma component import
work.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/nextjs/`, not in this one.
