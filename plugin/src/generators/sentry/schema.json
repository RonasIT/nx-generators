{
  "$schema": "https://json-schema.org/schema",
  "$id": "Sentry",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Enter the name of the directory in the 'apps/' folder (e.g: web)"
    },
    "DSN": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Enter the DSN"
    }
  },
  "required": ["directory", "DSN"]
}
