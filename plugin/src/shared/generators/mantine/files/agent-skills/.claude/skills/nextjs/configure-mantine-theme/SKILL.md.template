---
name: configure-mantine-theme
description: >
  Sync this app's Mantine theme with the design tokens defined in `_variables.scss`. Use whenever
  asked to "update the Mantine theme", "sync the theme with variables", "wire up theme
  colors/sizes", or similar. Doesn't touch fonts — see the `add-fonts` skill for that.
---

# Configure Mantine theme

This is a pointer skill. The full instructions live at
`.agents/skills/nextjs/configure-mantine-theme/SKILL.md` (Agent Skills convention, shared across coding
agents) — read that file and follow it in full before touching the Mantine theme.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/nextjs/`, not in this one.
