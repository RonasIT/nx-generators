import { useTranslations } from 'next-intl';
import { ReactElement } from 'react';

export default function Index(): ReactElement {
  const t = useTranslations('<%= sharedTranslationsKey %>.HOME_PAGE');

  return <div>{t('HOME_PAGE_TEXT')}</div>;
}
