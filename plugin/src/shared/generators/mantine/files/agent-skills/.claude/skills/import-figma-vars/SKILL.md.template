---
name: import-figma-vars
description: >
  Import design tokens from a Figma UI-kit page's "Variables" block into this app's shared styles
  library. Use whenever asked to "import Figma variables", "sync design tokens from Figma", "import
  figma colors/tokens", or similar.
---

# Import Figma variables

This is a pointer skill. The full instructions live at
`.agents/skills/import-figma-vars/SKILL.md` (Agent Skills convention, shared across coding agents)
— read that file and follow it in full before doing any Figma variables import work.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/`, not in this one.
