---
name: add-fonts
description: >
  Wire one or more typefaces into this app's root layout via next/font/google, and into the
  Mantine theme if this app uses Mantine. Use whenever asked to "add a font", "install a Google
  font", "wire up next/font", "import figma fonts", or similar.
---

# Add fonts

This is a pointer skill. The full instructions live at
`.agents/skills/nextjs/add-fonts/SKILL.md` (Agent Skills convention, shared across coding agents) — read
that file and follow it in full before wiring up any fonts.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/`, not in this one.
