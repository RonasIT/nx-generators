{
  "$schema": "https://json-schema.org/schema",
  "$id": "Sentry",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Enter the name of the directory in the 'apps/' folder (e.g: web)"
    },
    "technology": {
      "type": "string",
      "description": "What technology is used in your project?",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "enum": ["Next.js", "React Native"],
      "x-prompt": "What technology is used in your project?"
    }
  },
  "required": ["directory", "technology"]
}
