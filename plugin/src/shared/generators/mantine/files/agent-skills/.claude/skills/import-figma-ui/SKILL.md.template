---
name: import-figma-ui
description: >
  Import design tokens from a Figma UI-kit page's "Variables" block into this app's shared styles
  library, then hand off to sibling skills for assets/theme/fonts. Use whenever asked to "import
  Figma UI", "sync design tokens from Figma", "import figma variables/colors/fonts/assets", or
  similar.
---

# Import Figma UI

This is a pointer skill. The full instructions live at
`.agents/skills/import-figma-ui/SKILL.md` (Agent Skills convention, shared across coding agents)
— read that file and follow it in full before doing any Figma import work.

Do not duplicate or paraphrase its steps here; edits to the process belong in the source file
under `.agents/skills/`, not in this one.
