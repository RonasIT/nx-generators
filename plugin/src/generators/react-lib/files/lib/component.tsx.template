import React, { Fragment, ReactElement } from 'react';

interface <%= formatName(name, true) %>Props {
  // TODO: Describe props
}

export function <%= formatName(name, true) %>Component(props: <%= formatName(name, true) %>Props): ReactElement {
  // TODO: Implement component
  return <Fragment />;
}
