{
  "$schema": "https://json-schema.org/schema",
  "$id": "Mantine",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "The directory name of the app in the 'apps/' folder",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Enter the name of the directory in the 'apps/' folder"
    },
    "withFormComponents": {
      "type": "boolean",
      "description": "Add Form components to the UI-kit library (requires react-hook-form)",
      "default": false,
      "x-prompt": "Do you want to add Form components to the UI-kit library?"
    }
  },
  "required": ["directory", "withFormComponents"]
}
